# LineAR Concept B Application

## Installation

To install app dependencies, use the command `npm install`

## Running the application

To run the application once you have installed all dependencies, use the command `npm run dev`

Afterwards, the application should automatically start in your localhost with the address 3000, which should look like this `localhost:3000`
